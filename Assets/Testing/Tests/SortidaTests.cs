using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class SortidaTests
{
    public GameObject entitatTemporal;
    /*
    [Test]
    public void SortidaDestrueixUnaEntitat()
    {
        GameObject sortida = GameObject.Instantiate(Resources.Load("LlibreriaObjectes/Sortida/Sortida")) as GameObject;
        SortidaScript sortidaScript = sortida.GetComponent<SortidaScript>();
        entitatTemporal = GameObject.CreatePrimitive(PrimitiveType.Plane);
        
        sortidaScript.inicialitzaPerFerTests();
        //sortidaScript.recieveObject(entitatTemporal);

        Assert.That(sortidaScript.getNEntitatsDestruides(), Is.EqualTo(1));
    }
    */
}
